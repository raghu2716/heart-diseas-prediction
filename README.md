# heart-diseas-prediction'
## Project Snapshot

- Flask app with authentication, model prediction, and session-managed state.
- SQLite logging + KNN model flow (heart-disease-prediction-knn-model.pkl).
- PDF report generation at /download_pdf with symptoms + causes.
- Real output preview page at /output (image + video in templates/output.html).
- Docker support via Dockerfile and GitHub Actions CI pipeline (.github/workflows/python-app.yml).
'
